// Keep the public image URL deliberately neutral. Some browser content
// blockers reject image endpoints whose path contains formula/card keywords.
export {GET} from '@/app/api/formula-recommendations/thumbnail/route';
