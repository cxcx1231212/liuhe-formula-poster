import { DurableObject } from 'cloudflare:workers';export class PostViewCounter extends DurableObject {  constructor(ctx, env) {    super(ctx, env);    ctx.blockConcurrencyWhile(async () => {      this.ctx.storage.sql.exec('CREATE TABLE IF NOT EXISTS counter (id INTEGER PRIMARY KEY CHECK (id = 1), value INTEGER NOT NULL); INSERT OR IGNORE INTO counter (id, value) VALUES (1, 0);');    });  }  async read(increment) {    if (increment) this.ctx.storage.sql.exec('UPDATE counter SET value = value + 1 WHERE id = 1');    return this.ctx.storage.sql.exec('SELECT value FROM counter WHERE id = 1').one().value;  }}async function handleViewCount(request, env, url) {  if (request.method !== 'GET' && request.method !== 'POST') return new Response(null, { status: 405, headers: { allow: 'GET, POST' } });  const path = url.searchParams.get('path') || '';  if (!path.startsWith('/posts/') || path.length > 300 || /[\r\n]/.test(path)) return Response.json({ error: '无效帖子地址' }, { status: 400 });  const count = await env.VIEW_COUNTER.getByName(path).read(request.method === 'POST');  return Response.json({ count }, { headers: { 'cache-control': 'no-store' } });}const POST_PATH = /^\/posts\//;const CURRENT_TTL = 300;const INDEX_TTL = 300;const HISTORY_TTL = 604800;const CURRENT_KV_TTL = 3600;const INDEX_KV_TTL = 600;const HISTORY_KV_TTL = 60 * 60 * 24 * 90;const CACHE_VERSION = 'zodiac-history-v2';function isCacheableDocument(request, url) {  const isPage = url.pathname === '/' || POST_PATH.test(url.pathname);  return request.method === 'GET' && isPage &&    !request.headers.has('RSC') && !request.headers.has('Next-Router-Prefetch') &&    (request.headers.get('accept') || '').includes('text/html');}function cacheKey(url) {  const versioned = new URL(url);  versioned.searchParams.set('__formula_global_cache', CACHE_VERSION);  return new Request(versioned.toString(), { method: 'GET' });}function legacyCacheKey(url) {  return new Request(url.toString(), { method: 'GET' });}function globalKey(url) {  return 'html:' + CACHE_VERSION + ':' + url.pathname + url.search;}function legacyGlobalKey(url) {  return 'html:' + url.pathname + url.search;}function withSecurityHeaders(response) {  const headers = new Headers(response.headers);  headers.set('content-security-policy', "frame-ancestors 'none'");  headers.set('x-frame-options', 'DENY');  headers.set('x-content-type-options', 'nosniff');  headers.set('referrer-policy', 'strict-origin-when-cross-origin');  headers.set('permissions-policy', 'camera=(), microphone=(), geolocation=()');  return new Response(response.body, { status: response.status, statusText: response.statusText, headers });}const PAYLOAD_PARAM = '__formula_payload';function shellResponse(response, url) {  const payloadUrl = new URL(url);  payloadUrl.searchParams.set(PAYLOAD_PARAM, '1');  const payloadPath = payloadUrl.pathname + payloadUrl.search;  const headers = new Headers(response.headers);  headers.delete('content-length');  headers.delete('content-encoding');  headers.delete('etag');  headers.set('content-type', 'text/html; charset=utf-8');  headers.set('cache-control', 'no-store');  const html = '<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>六合公式库</title></head><body><script>fetch(' + JSON.stringify(payloadPath) + ',{credentials:"same-origin"}).then(function(r){if(!r.ok)throw new Error(r.status);return r.text()}).then(function(h){document.open();document.write(h);document.close()}).catch(function(){document.body.textContent="页面读取失败，请刷新重试"})<\/script></body></html>';  return withSecurityHeaders(new Response(html, { status: response.status, statusText: response.statusText, headers }));}function deliverDocument(response, url, payloadRequest) {  return payloadRequest ? response : shellResponse(response, url);}function kindOf(headers) { return headers.get('x-formula-edge-cache-kind') || headers.get('x-formula-cache-kind') || 'current'; }function ttlFor(kind) { return kind === 'history' ? HISTORY_TTL : kind === 'index' ? INDEX_TTL : CURRENT_TTL; }function kvTtlFor(kind) { return kind === 'history' ? HISTORY_KV_TTL : kind === 'index' ? INDEX_KV_TTL : CURRENT_KV_TTL; }function makeResponse(body, sourceHeaders, edgeState, globalState, kind) {  const headers = new Headers(sourceHeaders);  headers.delete('set-cookie');  headers.delete('vary');  headers.set('cache-control', 'public, max-age=0, s-maxage=' + ttlFor(kind));  headers.set('x-formula-edge-cache', edgeState);  headers.set('x-formula-edge-cache-kind', kind);  headers.set('x-formula-global-cache', globalState);  return withSecurityHeaders(new Response(body, { status: 200, headers }));}async function storeGlobal(env, url, response, kind) {  const html = await response.text();  await env.HTML_CACHE.put(globalKey(url), html, {    expirationTtl: kvTtlFor(kind),    metadata: { kind, contentType: response.headers.get('content-type') || 'text/html; charset=utf-8' },  });}export default {  async fetch(request, env, ctx) {    const url = new URL(request.url);    const payloadRequest = url.searchParams.get(PAYLOAD_PARAM) === '1';    if (payloadRequest) {      url.searchParams.delete(PAYLOAD_PARAM);      request = new Request(url.toString(), request);    }    if (url.pathname === '/api/view-count') return withSecurityHeaders(await handleViewCount(request, env, url));    if (!isCacheableDocument(request, url)) return withSecurityHeaders(await env.APP.fetch(request));    const localKey = cacheKey(url);    ctx.waitUntil(Promise.all([      caches.default.delete(legacyCacheKey(url)),      env.HTML_CACHE.delete(legacyGlobalKey(url)),    ]));    const cached = await caches.default.match(localKey);    if (cached) {      const kind = kindOf(cached.headers);      if (cached.headers.get('x-formula-global-cache') !== 'HIT') ctx.waitUntil(storeGlobal(env, url, cached.clone(), kind));      return deliverDocument(makeResponse(cached.body, cached.headers, 'HIT', cached.headers.get('x-formula-global-cache') || 'FILLING', kind), url, payloadRequest);    }    const stored = await env.HTML_CACHE.getWithMetadata(globalKey(url), { type: 'text', cacheTtl: 60 });    if (stored.value !== null) {      const kind = stored.metadata?.kind || (url.pathname === '/' ? 'index' : 'history');      const response = makeResponse(stored.value, {        'content-type': stored.metadata?.contentType || 'text/html; charset=utf-8',      }, 'HIT', 'HIT', kind);      ctx.waitUntil(caches.default.put(localKey, response.clone()));      return deliverDocument(response, url, payloadRequest);    }    const origin = await env.APP.fetch(request);    if (origin.status !== 200 || !origin.headers.get('content-type')?.includes('text/html')) return withSecurityHeaders(origin);    const kind = url.pathname === '/' ? 'index' : kindOf(origin.headers);    const response = makeResponse(origin.body, origin.headers, 'MISS', 'MISS', kind);    ctx.waitUntil(Promise.all([      caches.default.put(localKey, response.clone()),      storeGlobal(env, url, response.clone(), kind),    ]));    return deliverDocument(response, url, payloadRequest);  },};
